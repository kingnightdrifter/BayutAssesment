package com.katch.hi5.ui.main.views.dialogs

import android.content.Context
import android.content.DialogInterface
import androidx.appcompat.app.AlertDialog
import com.bayut.bayutassignemnt.R


// Generic Yes/No Dialogue
object GenericDialogues {
/*    fun showDialogue(
        context: Context?,
        title: String?,
        message: String?,
        yes: String?,
        no: String?,
        onYesNoClickListener: OnYesNoClickListener
    ) {
        AlertDialog.Builder(context!!, R.style.AlertDialogTheme)
            .setTitle(title)
            .setMessage(message)
            .setPositiveButton(yes) { dialog: DialogInterface?, which: Int -> onYesNoClickListener.onYesClick() }
            .setNegativeButton(no) { dialog: DialogInterface, which: Int -> dialog.cancel() }
            .show()
    }*/

}